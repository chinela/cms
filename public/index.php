<?php

    require_once '../app/bootstrap.php';

    $router = new Router();
   
    // Redirect::to('admin/dashboard');
    // $he = DB::connect()->query("UPDATE categories SET cat_user = 'PRO' WHERE cat_id = 2");
    // DB::connect()->update('categories','cat_id', 2, array(
    //     'cat_user' => 'program'
    // ));

